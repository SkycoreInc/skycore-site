name: Auto MS How-To Generator

on:
  schedule:
    - cron: '0 12 */4 * *'  # Every 4 days at 12 PM UTC (offset from blog @ 10AM, gen1 @ 11AM)
  workflow_dispatch:          # Manual trigger: Actions tab → Run workflow

permissions:
  contents: write
  pull-requests: write

jobs:
  generate:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v4
        with:
          ref: dev

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --upgrade google-genai requests

      - name: Check API keys
        run: |
          if [ -z "${{ secrets.GEMINI_API_KEY }}" ]; then
            echo "ERROR: GEMINI_API_KEY secret is not set."
            exit 1
          fi
          echo "GEMINI_API_KEY found OK"
          if [ -z "${{ secrets.PEXELS_API_KEY }}" ]; then
            echo "WARNING: PEXELS_API_KEY not set — images will use Unsplash fallback"
          else
            echo "PEXELS_API_KEY found OK"
          fi

      - name: Generate MS how-to article
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python scripts/generate_ms_howto.py

      - name: Commit and push to dev
        run: |
          git config user.name "SkyCore MS HowTo Bot"
          git config user.email "info@skycoresolutions.com"
          git add -A
          git diff --staged --quiet || git commit -m "Auto: new MS how-to guide $(date +%Y-%m-%d)"
          for i in 1 2 3 4 5; do
            git pull --rebase origin dev && git push origin dev && break
            echo "Push attempt $i failed — retrying in 10s..."
            sleep 10
          done

      - name: Open PR to main (if none exists)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EXISTING=$(gh pr list --base main --head dev --state open --json number --jq '.[0].number')
          if [ -z "$EXISTING" ]; then
            gh pr create \
              --base main \
              --head dev \
              --title "Auto: new MS how-to guide $(date +%Y-%m-%d)" \
              --body "🤖 New Microsoft how-to article generated by the automated MS how-to bot. Review the diff and merge to publish to the live site."
            echo "PR created ✓"
          else
            echo "PR #$EXISTING already open — skipping creation"
          fi
